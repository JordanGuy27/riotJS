<cta>
  <button class="button-primary" name={opts.name}>
    {opts.label}
  </button>


  //this is only required when the mixin is not defined globally
  <script> this.mixin('RequiredOpt');</script>
</cta>
